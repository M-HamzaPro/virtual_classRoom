<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fypwebhost">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_con"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CosineComparing"></activity>
        <activity android:name=".UploadingAssignment" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ChangePasswordCode" />
        <activity android:name=".CurrentClass" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Create_Class" />
        <activity android:name=".Teacher_Activity" />
        <activity android:name=".Student_Activity" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>